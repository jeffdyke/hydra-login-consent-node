serve:
  cookies:
    same_site_mode: None
    domain: "https://auth.staging.bondlink.org"
    same_site_workaround: true



urls:
  self:
    issuer: http://hydra:4444
  consent: http://127.0.0.1:3000/consent
  login: http://127.0.0.1:3000/login
  logout: http://127.0.0.1:3000/logout

oidc:
  subject_identifiers:
    supported_types:
      - pairwise
      - public
    pairwise:
      salt:GOOGLE_REDIRECT_URI="https://auth.staging.bondlink.org/callback"

HYDRA_ADMIN_URL=http://10.1.1.230:4445
HYDRA_URL=https://auth.staging.bondlink.org
POSTGRES_HOST=10.1.1.230
BASE_URL=https://auth.staging.bondlink.org
URLS_SELF_ISSUER=https://auth.staging.bondlink.org
URLS_CONSENT=https://auth.staging.bondlink.org/consent
URLS_LOGIN=https://auth.staging.bondlink.org/login
BASE_URL=https://auth.staging.bondlink.org
REDIRECT_URL=https://auth.staging.bondlink.org/callback
NODE_ENV=develovp
SERVE_COOKIES_DOMAIN=bondlink.org

SERVE_COOKIES_SAME_SITE_WORKAROUND=true
SERVE_PUBLIC_CORS_ENABLED=true
SERVE_ADMIN_CORS_ENABLED=true
SERVE_PUBLIC_CORS_ALLOWED_ORIGINS="*"
SERVE_ADMIN_CORS_ALLOWED_ORIGINS="*"
DSN=postgres://hydra:shaken!stirred@10.1.1.230:5432/hydra?sslmode=disable
SERVE_PUBLIC_CORS_ALLOWED_METHODS=POST,GET,PUT,DELETE
SERVE_ADMIN_CORS_ALLOWED_METHODS=POST,GET,PUT,DELETE
OAUTH2_EXPOSE_INTERNAL_ERRORS=true
LOG_LEAK_SENSITIVE_VALUES=true
LOG_LEVEL=info
LOG_FORMAT=json
